# catalog: CSV waste_code exact matching (default, no model download)
# chroma: existing Chroma index + local sentence-transformers embedding
ENERGY_RAG_MODE=catalog
# Default database location: runtime/energy-coach.sqlite3
# ENERGY_DB_PATH=C:/path/to/energy-coach.sqlite3
