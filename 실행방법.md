# 실행 방법

저장소를 새로 받은 사람이 Windows에서 서버를 띄우기까지의 순서입니다. 자세한 기능 설명은 [README.md](README.md)를 참고하세요.

## 준비물

- Python 3.10 이상 (`python --version`으로 확인)
- 인터넷 연결 (화면이 Tailwind CDN과 웹 글꼴을 불러옵니다)
- Node.js는 화면 테스트를 돌릴 때만 필요합니다. 서버 실행에는 필요 없습니다.

## 1. 저장소 받기

```powershell
git clone https://github.com/pa3757/0917_AI-Challengers_6.git
cd 0917_AI-Challengers_6
```

## 2. 처음 한 번만: 가상환경과 패키지 설치

`.venv/`와 `node_modules/`는 Git에 올라가지 않으므로 각자 만들어야 합니다.

```powershell
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
```

`requirements-dev.txt`는 실행용 `requirements.txt`에 테스트 도구(pytest, httpx)를 더한 목록입니다. 실행만 할 거라면 `requirements.txt`를 설치해도 됩니다.

## 3. 서버 실행

```powershell
.venv\Scripts\python.exe -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

- 화면: http://127.0.0.1:8000
- API 문서: http://127.0.0.1:8000/docs
- 종료: 실행한 터미널에서 `Ctrl+C`

`energy-coach.html`을 더블클릭해서 열지 말고 반드시 위 주소로 접속하세요. `--reload`가 켜져 있어 코드를 저장하면 서버가 자동으로 다시 시작됩니다.

PowerShell 스크립트 실행이 허용된 환경이라면 `.\run.ps1`로도 같은 명령을 실행할 수 있습니다.

## 따로 공유하지 않아도 되는 파일

아래는 `.gitignore`로 제외되지만 각자 만들거나 자동으로 생깁니다.

| 항목 | 만드는 방법 |
|---|---|
| `.venv/` | 2단계에서 생성 |
| `node_modules/` | `npm.cmd ci --ignore-scripts` (테스트할 때만) |
| `runtime/energy-coach.sqlite3` | 서버 첫 실행 시 자동 생성. 각자의 설문 응답·진단 기록이 저장됩니다 |
| `.env` | 없어도 됩니다. 설정을 바꿀 때만 `.env.example`을 복사해서 사용 |

## 테스트

```powershell
# API 테스트
.venv\Scripts\python.exe -m pytest tests visualstudio_project/tests -q

# 화면(DOM) 테스트
npm.cmd ci --ignore-scripts
$env:PYTHONIOENCODING = 'utf-8'
npm.cmd test
```

## 자주 겪는 문제

**포트 8000이 이미 사용 중이라며 서버가 바로 꺼짐** (`WinError 10013`)
다른 터미널에서 서버가 이미 실행 중입니다. 그 터미널에서 `Ctrl+C`로 끄거나, 아래로 프로세스를 찾아 종료하세요.

```powershell
Get-NetTCPConnection -LocalPort 8000 -State Listen | Select-Object OwningProcess
Stop-Process -Id <위에서 나온 번호>
```

**`npm test`가 `UnicodeEncodeError: 'cp949' codec` 오류로 실패함**
한글 Windows의 기본 인코딩 문제입니다. 같은 터미널에서 `$env:PYTHONIOENCODING = 'utf-8'`을 먼저 실행하세요.

**pytest가 `PermissionError: ... pytest-of-<사용자>` 오류로 실패함**
임시 폴더 권한 문제입니다. 쓰기 가능한 폴더를 지정하세요.

```powershell
.venv\Scripts\python.exe -m pytest tests -q --basetemp="$env:TEMP\energy-coach-pytest"
```

**git 명령이 `detected dubious ownership` 오류로 거부됨**
폴더를 만든 사용자와 현재 사용자가 다를 때 생깁니다. 한 번만 실행하면 됩니다.

```powershell
git config --global --add safe.directory <프로젝트 폴더 경로>
```

## 선택: Chroma 의미 검색 모드

기본값인 `catalog` 모드는 CSV를 정확히 매칭하므로 추가 설치가 필요 없습니다. Chroma 의미 검색을 쓰려면 임베딩 모델을 처음 한 번 내려받아야 합니다.

```powershell
.venv\Scripts\python.exe -m pip install -r visualstudio_project/requirements.txt
.venv\Scripts\python.exe visualstudio_project/scripts/build_rag_index.py
$env:ENERGY_RAG_MODE = 'chroma'
.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```
