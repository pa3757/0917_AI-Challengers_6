# Energy Coach 기능 연결 현황

작성일: 2026-09-17  
확인 기준: 현재 `app/`, `static/energy-coach.js`, `energy-coach.html`, `visualstudio_project/src/` 구현 코드

## 1. 현재 구현 범위

**설문 → 위험 진단 → 솔루션 추천 → 근거 표시 → 개별 예상 절감 효과 → 실천 완료 기록** 흐름은 FastAPI 단일 서버에 연결되어 있습니다.

실시간 계측, 실제 청구액, 월별 추이, IoT 제어는 연결되지 않았습니다. 현재 화면의 예상 절감액은 계산 엔진의 결과이며 실제 절감 실적이 아닙니다.

| 상태 | 의미 |
| --- | --- |
| 연결 완료 | 화면에서 입력·조회할 수 있고 서버 처리까지 연결됨 |
| 조건부 연결 | 호출 경로는 있으나 추가 환경 설정·데이터·실행 검증이 필요함 |
| 미구현 | 실제 처리 로직 또는 데이터 연결이 없어 추가 개발이 필요함 |
| 선택 확장 | 현재 설문 기반 서비스를 실행하는 데 필수는 아니지만 서비스 범위 확장 시 필요함 |

## 2. 연결 완료 기능

| 기능 | 현재 동작 | 구현 위치 / API | 현재 범위 및 제한 |
| --- | --- | --- | --- |
| 단일 서버 화면 제공 | FastAPI가 HTML과 JavaScript 제공 | `app/main.py` · `GET /`, `/static/energy-coach.js` | HTML 직접 열기가 아닌 서버 주소 접속 필요 |
| 화면 이동 | 시작·설문·진단·가이드·효과·상세·내 정보 이동 | `static/energy-coach.js` · `goTo`, `goBack` | 진단 결과가 필요한 화면은 설문 응답을 먼저 확인 |
| 실제 설문 문항 표시 | 기존 JSON의 17개 문항, 선택지와 숫자 입력 표시 | `GET /api/survey` · `survey_loader.py` | 기본 정보 탭 추가, 주방 예시 탭은 실제 문항에 맞춰 운영 관리로 변경 |
| 설문 진행률 | 필수 문항 응답 수에 따라 진행률 갱신 | `updateSurveyProgress` | 선택 문항은 필수 진행률에서 제외 |
| 입력 검증 | 필수 응답·선택 코드·숫자 범위·좌석 정수 등 확인 | `app/service.py` · `POST /api/diagnosis` | 브라우저 검사와 서버 검증 적용 |
| 측정값 수동 입력 | 기존 측정 항목 정의에 따라 선택 입력란 제공 | `renderSurvey`, `validate_measurements` | 자동 계측 수집은 없으며 사용자가 입력해야 함 |
| 위험 진단 | 기존 단일·복합 규칙으로 낭비 요인, 위험 점수, 사유 산출 | `rule_engine.py` → `POST /api/diagnosis` | 내부 진단 지표이며 공식 에너지 효율등급은 아님 |
| 솔루션 매칭 | 진단된 낭비 코드에 맞는 기존 솔루션 조회 | `solution_engine.py` | 등록된 카탈로그 범위 내에서 추천 |
| 기본 근거 조회 | CSV에서 낭비 코드를 정확히 매칭해 근거 연결 | `app/service.py` · `ground_solutions` | 기본 `catalog` 모드. 벡터 의미 검색이나 LLM 생성은 아님 |
| 추천 우선순위 | 기존 순위 엔진으로 최대 3개 추천 선정 | `ranking_engine.py` | 근거 유무, 계산 상태, 위험 점수와 중복 그룹 등을 반영 |
| MVP 예상 절감량 | 기존 내부 가정값과 계산값으로 참고 범위 산출 | `mvp_calculation_engine.py` | 기본 활성화. 요금은 130원/kWh 가정이며 실제 청구액 아님 |
| 측정·입력 기반 계산 | MVP 참고 추정을 끄면 기존 정량 계산 엔진 사용 | `calculator.py` | 입력 부족 항목은 추가 입력 필요로 표시 |
| 요금 조건 선택 | 저장된 계약·전압·계절·시간대 조건을 선택하여 계산에 전달 | `GET /api/survey`, `tariff_id` | 참고 추정을 끈 경우 적용. 기존 DB 저장 요율이며 자동 갱신 없음 |
| 결과 카드 표시 | 실제 위험 사유와 추천별 kWh·원 범위 표시 | `renderDiagnosis`, `renderResults` | 상단 효과는 1순위 추천 기준. 전체 절감량 합산은 하지 않음 |
| 상세 근거 표시 | 출처 링크·문서 내용·계산 가정·부족 입력 표시 | `renderSolutions`, `evidenceHtml` | 기존 데이터에 담긴 근거 사용 |
| 미션 완료 저장 | 추천 미션의 완료 여부를 저장·복원 | `PUT /api/missions/{solution_id}` | 체크는 실천 기록이며 설비 제어 명령이 아님 |
| 업장명 저장 | 내 정보에서 업장명 수정 및 저장 | `PUT /api/profile` | 프로필 편집은 업장명만 지원. 좌석 수는 설문 값 사용 |
| 진단 결과 복원 | 제출한 설문·측정값·요금 조건·최신 결과·미션 복원 | `GET /api/state` · `app/store.py` | SQLite와 브라우저 쿠키 기반. 로그인 계정 동기화는 아님 |
| 재진단 | 수정한 입력으로 다시 계산하고 결과 갱신 | `POST /api/diagnosis` | 이전 진단을 최신 결과로 대체하고 미션 완료 상태 초기화 |
| 오류 안내 | 입력 오류, 연결 실패, 요청 시간 초과 등 표시 | `api`, `message` | 진단 처리 실패 시 기존 저장 결과 보존 |
| 상태 확인·API 문서 | 서버 응답 확인과 API 명세 조회 | `GET /api/health`, `/docs` | `/api/health`는 단순 상태 응답이며 DB·모델 종합 점검은 아님 |

## 3. 조건부 연결 기능

### Chroma 기반 벡터 근거 검색

- **현재 상태:** 기존 검색 엔진을 FastAPI에서 호출하도록 연결한 상태입니다. 실제 모델 다운로드·인덱스 구축 후 전체 검색 동작은 검증하지 않았습니다.
- **기본 동작:** CSV의 정확한 낭비 코드 매칭을 사용하므로 이 설정 없이도 진단 가능합니다.
- **추가 작업:** 선택 의존성 설치, 임베딩 모델 준비, 인덱스 구축, 환경변수 설정, 실제 검색 결과와 응답 시간 검증.
- **관련 코드:** `app/service.py`, `visualstudio_project/src/retriever.py`, `rag_indexer.py`, `embedding_provider.py`.

```powershell
.venv\Scripts\python.exe -m pip install -r visualstudio_project/requirements.txt
.venv\Scripts\python.exe visualstudio_project/scripts/build_rag_index.py
$env:ENERGY_RAG_MODE = 'chroma'
.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

## 4. 미연결 기능과 추가해야 할 작업

아래는 원본 화면에서 표현하던 기능을 실제 데이터로 제공하려면 필요한 작업입니다. 화면에 카드가 있더라도 실제 기능이 연결된 것으로 보지 않습니다.

| 기능 | 현재 화면 / 상태 | 추가해야 할 작업 | 필요한 데이터·선행 조건 |
| --- | --- | --- | --- |
| 실시간 전력 계측 | 실시간 가동 상태나 계측 수치를 제공하지 않음 | 계측 수집 API 또는 장치 연동, 측정 시각·단위·장치 매핑 저장, 최신값 조회 및 화면 갱신 | 스마트미터·전력량계 또는 사용 가능한 외부 계측 API |
| 월별 사용량·요금 추이 | 월별 이력 없음 안내와 현재 설문 사용량만 표시 | 월별 입력·조회 API, 월별 저장 테이블, 실제 데이터 기반 차트 | 업장별 월 사용량·청구액·청구 기간 |
| 실제 누적 절감액·절감량 | 실제 실적을 계산하지 않음 | 개선 전 기준 사용량 설정, 개선 후 실적 비교, 기간 및 운영 조건 차이 반영 | 개선 전후 계측·요금 데이터와 비교 기준 |
| 이번 달 예상 청구액 | 산정 자료 없음 표시 | 기본요금·전력량요금·적용 조정 항목·세금 등을 포함한 별도 청구 추정 로직 | 계약 정보, 계절·시간대별 사용량, 피크전력, 적용 요율 |
| 탄소 감축량 | 산정하지 않음 표시 | 배출계수와 적용 기간·단위 관리, 절감 kWh에 따른 환산 로직 및 출처 표시 | 사용할 배출계수와 근거, 추정 또는 실측 절감량 구분 |
| 계약전력 대비 피크 여유 | 원본 예시 지표를 실제 수치로 제공하지 않음 | 계약전력 입력·저장, 피크전력 집계, 여유 계산 및 표시 | 계약전력과 같은 기준의 피크 측정값 |
| 동종 업장 대비 백분위 | 원본의 고정 백분위 값 제거 | 비교 집단 정의, 규모·영업시간 보정, 백분위 계산 | 비교 가능한 업장 통계 및 산정 기준 |
| 주방·조리기기 전용 진단 | 해당 예시 탭을 운영 관리로 변경 | 주방 문항·규칙·솔루션·공식 근거·계산식 추가 후 탭 연결 | 조리기기 운전 특성, 소비전력 및 권고 근거 |
| IoT 자동 제어·스케줄 | 원격 제어 기능 없음. 완료 체크만 저장 | 장치 등록, 제어 API, 실행 상태 확인, 예약·실패 처리, 제어 이력 구현 | 연동 가능한 스마트플러그·제어 장치와 제어 규격 |
| 전체 추천 적용 시 총 절감액 | 개별 수치만 표시하고 합산하지 않음 | 동일 설비·시간대·부하에 대한 중복 효과를 제거하는 통합 시나리오 계산 | 추천 간 효과 중복 관계 및 일관된 기준 사용량 |

## 5. 선택적으로 확장할 기능

현재 설문 기반 서비스를 실행하는 데 필수는 아니며, 실제 서비스 목표에 따라 추가 여부를 결정합니다.

| 기능 | 현재 상태 | 추가 작업 |
| --- | --- | --- |
| LLM 상담·개인화 설명 생성 | 원본 `llm_service.py`가 빈 파일. 현재 출력은 규칙·계산·근거 데이터 기반 | 모델 연결, 근거 기반 설명 생성, 계산값 보존, 오류·비용·응답 시간 관리, 질문 입력 UI/API |
| 계정 로그인·기기 간 복원 | 브라우저 쿠키 기반 익명 저장만 제공 | 인증, 계정과 업장 연결, 로그인 사용자별 조회·수정 권한 |
| 여러 업장 관리 | 브라우저 세션당 하나의 최신 상태 | 업장 테이블, 업장 선택 UI, 업장별 데이터 분리 |
| 진단·실천 이력 관리 | 최신 결과만 저장. 완료 여부만 저장하며 완료 일시 없음 | 진단별 버전·생성 시각, 미션 완료 시각·변경 이력, 이전 결과 조회 |
| 작성 중 설문 자동 저장 | 진단 제출에 성공한 응답만 서버 저장 | 임시 응답 저장 API, 자동 저장 상태 표시, 재접속 시 초안 복원 |
| 요금표·근거 자료 갱신 | 제공된 파일의 저장값 사용 | 버전·유효기간 관리, 갱신 절차 및 변경 검증 |
| 리포트 다운로드 | 조회 화면만 제공 | PDF 또는 CSV 내보내기 API와 다운로드 버튼 |

## 6. 화면별 요약

| 화면 | 연결 완료 | 남은 작업 |
| --- | --- | --- |
| 시작 | 서비스 진입, 설문 화면 이동 | 실제 브라우저 시각 확인 |
| 설문 | 17개 문항, 진행률, 수동 측정값, 요금 선택, 진단 제출 | 주방 전용 설문 추가, 필요 시 초안 자동 저장 |
| 진단 | 위험 요인 개수·사유·내부 점수 | 실제 효율 지표·피크 분석·동종 업장 비교 |
| 가이드 | 우선순위 추천, 근거 표시, 완료 기록 | 실천 이력 및 장치 연동이 필요한 경우 별도 개발 |
| 절감 효과 | 추천별 개별 예상 kWh·금액 | 탄소 환산, 중복 제거 후 통합 효과 계산 |
| 상세 솔루션 | 전체 솔루션 근거·가정·추가 입력 안내 | 실제 장치 제어·스케줄 실행 |
| 내 정보 | 업장명, 좌석 수, 제출한 월 사용량, 완료 미션 수 | 월별 차트, 실제 누적 절감 실적, 예상 청구액 |

## 7. 권장 추가 작업 순서

1. **실제 브라우저 확인:** PC·모바일 화면에서 카드 배치, 스크롤, 입력과 화면 전환을 확인합니다.
2. **실적 데이터 기반 마련:** 월별 사용량·청구액 수동 등록과 이력 저장을 먼저 구현합니다. 계측 자동 연동 전에도 실제 데이터 차트를 제공할 수 있습니다.
3. **실적 계산 연결:** 비교 기준을 정한 뒤 절감 추이·누적 실적·청구 추정을 구현합니다. 탄소 환산도 적용할 계수를 정한 후 연결합니다.
4. **진단 범위 확장:** 주방 진단 데이터와 규칙을 추가합니다. 의미 검색이 필요한 경우 Chroma 인덱스 구축과 실제 검색 검증을 진행합니다.
5. **계정·장치·LLM 확장:** 여러 업장 운영, 기기 간 복원, 자동 제어, 대화형 설명이 필요한 범위에 맞춰 선택적으로 구현합니다.

이 순서는 개발 제안이며, 위 항목이 모두 현재 서비스 실행의 필수 조건이라는 의미는 아닙니다.

## 8. 검증 현황과 한계

- 이전 구현 단계에서 Python 테스트 **70개**, DOM 기반 화면 동작 테스트 **3개**가 통과했습니다.
- 실제 서버의 HTML·JavaScript·설문 API·상태 API(`/api/health`)·API 문서 경로가 HTTP 200으로 응답하는 것을 확인했습니다.
- DOM 테스트는 화면 이동·입력 전달·결과 표시 구조·재진단·오류 재시도를 검증합니다. 실제 브라우저에서의 시각적 검증을 대신하지 않습니다.
- 연결 가능한 브라우저가 없어 실제 화면의 시각 검증은 미완료입니다.
- Chroma 실제 모델·인덱스 검색, 외부 계측 API, IoT 장치 동작은 검증하지 않았습니다.
- 이 문서 작성 시에는 구현 코드를 확인했으며 테스트를 새로 실행하지 않았습니다.

## 9. 관련 파일

- [실행 및 설정 안내](README.md)
- [FastAPI API 정의](app/main.py)
- [진단 파이프라인 연결](app/service.py)
- [SQLite 상태 저장](app/store.py)
- [화면 연결 코드](static/energy-coach.js)
- [HTML 디자인](energy-coach.html)
- [API 테스트](tests/test_api.py)
- [화면 동작 테스트](tests/frontend.test.cjs)
